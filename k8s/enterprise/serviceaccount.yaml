apiVersion: v1
kind: ServiceAccount
metadata:
  name: notiflex-api
  namespace: enterprise
  annotations:
    # notiflex 네임스페이스와 같은 GCP 서비스 계정을 빌려 쓴다.
    # GCP 쪽에도 enterprise/notiflex-api 를 workloadIdentityUser 로 바인딩해야 동작한다.
    iam.gke.io/gcp-service-account: notiflex-secrets@project-b3c5c78c-8a5c-4e47-9fe.iam.gserviceaccount.com
