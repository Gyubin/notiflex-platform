apiVersion: v1
kind: Service
metadata:
  name: notiflex-api
  namespace: notiflex
spec:
  type: ClusterIP
  selector:
    app: notiflex-api
  ports:
    - name: http
      appProtocol: http
      port: 8080
      targetPort: 8080
