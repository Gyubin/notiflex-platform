apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: notiflex-api
  namespace: notiflex
spec:
  # ch7.2에서 replicas를 2로 복원하면서 원래 값으로 되돌렸다.
  # api-pool은 노드 1개이므로, 그 노드를 드레인해야 할 때는 이 값을 임시로 0으로 낮춘다.
  minAvailable: 1
  selector:
    matchLabels:
      app: notiflex-api
